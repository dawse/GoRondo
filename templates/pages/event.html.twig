{% extends 'base.html.twig' %}

{% block title 'event' %}

{% block body %}
<div class="jumbotron">
<div class="container">
{{ form_start(form) }}
<div class="form-row">
<div class="col">
{{ form_row(form.maxPrice) }}
</div>
<div class="col">
<button class="btn btn-primary">Recherche</button>
</div>
</div>

{{ form_end(form) }}
</div>
</div>
<div class="container mt-4">
<h1> Events </h1>

<div class="row">
{% for Event in event %}
    <div class="col-md-4">
    {% include 'pages/event_property.html.twig' %}
    </div>
    {% endfor %}

    </div>

<div class="navigation">
    {{ knp_pagination_render(event) }}
</div>

</div>


{% endblock %}


