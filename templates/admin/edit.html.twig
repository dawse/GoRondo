{% extends 'base.html.twig' %}

{% block title %}Gérer les évenements {% endblock %}

{% block body %}
<div class="container mt-4">
<h2> Editer les evnenements </h2>
    {{form_start(form)}}
    {{form_widget(form)}}
    <br>
    <button class="btn btn-primary"> Editer </button>
    {{form_end(form)}}
</div>
{% endblock %}