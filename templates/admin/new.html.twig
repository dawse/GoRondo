{% extends 'base.html.twig' %}

{% block title %}Gérer les évenements {% endblock %}

{% block body %}
<div class="container mt-4">
<h2> Créer un nouveau evnenement </h2>
    {{form_start(form)}}
    {{form_widget(form)}}
    <br>
    <button class="btn btn-primary"> Créer </button>
    {{form_end(form)}}
</div>
{% endblock %}